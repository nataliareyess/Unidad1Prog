import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.v1.ph',
  appName: 'evaluacion-1-ph',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
